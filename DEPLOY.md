# دليل النشر على GitHub Pages

## 🚀 خطوات النشر السريعة

### 1. إعداد GitHub Repository

1. **أنشئ حساب GitHub** إذا لم يكن لديك:
   - اذهب إلى [github.com](https://github.com)
   - سجل حساب جديد أو سجل الدخول

2. **أنشئ repository جديد**:
   - اضغط على "+" في أعلى الصفحة
   - اختر "New repository"
   - اسم الـ repository: `queue-management-system`
   - اجعله Public
   - لا تضيف README أو .gitignore (لدينا ملفات جاهزة)
   - اضغط "Create repository"

### 2. رفع الملفات

#### الطريقة A: باستخدام Git (مُوصى بها)

1. **ثبت Git** على جهازك من [git-scm.com](https://git-scm.com)

2. **افتح Terminal/Command Prompt** وانتقل إلى مجلد المشروع:
```bash
cd /path/to/queue-management-system
```

3. **أعد Git**:
```bash
git init
git add .
git commit -m "Initial commit: Complete queue management system"
```

4. **اربط بالـ repository**:
```bash
git remote add origin https://github.com/YOUR-USERNAME/queue-management-system.git
```

5. **ارفع الملفات**:
```bash
git push -u origin main
```

#### الطريقة B: رفع يدوي

1. **في صفحة الـ repository** على GitHub
2. **اضغط على "uploading an existing file"**
3. **اسحب جميع الملفات** من مجلد المشروع
4. **اكتب رسالة commit**: "Initial commit"
5. **اضغط "Commit changes"**

### 3. تفعيل GitHub Pages

1. **اذهب إلى Settings** في الـ repository
2. **ابحث عن "Pages"** في القائمة الجانبية
3. **اختر المصدر**:
   - Source: "Deploy from a branch"
   - Branch: "main"
   - Folder: "/ (root)"
4. **اضغط Save**

### 4. الانتظار والتحقق

- **انتظر 5-10 دقائق** حتى يتم النشر
- **ستظهر رسالة خضراء** عندما يكون الموقع جاهزاً
- **الرابط سيكون**: `https://YOUR-USERNAME.github.io/queue-management-system/`

### 5. اختبار النظام

1. **افتح الرابط** في المتصفح
2. **اختبر جميع الصفحات**:
   - الصفحة الرئيسية: `/`
   - لوحة الإدارة: `/admin.html`
   - شاشة العرض: `/display.html`
   - لوحة التحكم: `/control.html`
   - الطباعة: `/print.html`
   - العميل: `/client.html`
   - معلومات: `/about.html`

## 📋 ملاحظات مهمة

### قبل النشر:
1. **تأكد من أن index.html موجود** في المجلد الرئيسي
2. **لا تغير اسم الملفات** (خاصة index.html)
3. **احتفظ ببنية المجلدات** كما هي
4. **لا تحذف أي ملفات** حتى لو بدا غير مستخدم

### بعد النشر:
1. **الرابط الرئيسي**: `https://YOUR-USERNAME.github.io/queue-management-system/`
2. **صفحات النظام**:
   - الرئيسية: `/`
   - الإدارة: `/admin.html`
   - العرض: `/display.html`
   - التحكم: `/control.html`
   - الطباعة: `/print.html`
   - العميل: `/client.html`
   - معلومات: `/about.html`

### استكشاف الأخطاء:
- **إذا لم يعمل**: تأكد من أن index.html في المجلد الرئيسي
- **إذا ظهر 404**: تأكد من اسم الملفات والمسارات
- **إذا لم يعمل التصميم**: تأكد من اتصال الإنترنت (Tailwind CSS)

## 🎯 نصائح سريعة

### للحصول على أفضل أداء:
1. **استخدم custom domain** (اختياري):
   - اذهب إلى Settings → Pages
   - أضف domain خاص بك
   - اتبع التعليمات لإعداد الـ DNS

2. **تحسين الأداء**:
   - استخدم صور مخفضة
   - ضغط ملفات الفيديو
   - استخدم CDN للموارد الخارجية

3. **الأمان**:
   - فعل HTTPS (يتم تلقائياً)
   - لا تضع معلومات حساسة في الكود
   - استخدم بيئة إنتاج منفصلة

## 🔧 التخصيص السريع

### تغيير الاسم:
1. عدل `index.html` - العنوان الرئيسي
2. عدل `admin.html` - اسم المركز من الإعدادات
3. عدل `display.html` - اسم المركز في الشاشة

### تغيير الألوان:
1. ابحث عن `bg-blue-600` في الملفات
2. استبدلها بالألوان المفضلة من Tailwind CSS
3. يمكنك استخدام أي لون من لوحة Tailwind

### تغيير الشعار:
1. ابحث عن `<i class="fas fa-hospital">`
2. استبدلها بأي أيقونة من Font Awesome
3. أو أضف صورة شعارك الخاصة

## 📞 الدعم

إذا واجهت مشكلة في النشر:
1. تحقق من [GitHub Pages documentation](https://docs.github.com/en/pages)
2. تأكد من أن جميع الملفات مرفوعة
3. جرب متصفح مختلف
4. افتح Issue في الـ repository

---

**مبروك! 🎉**

لقد قمت بنشر نظام إدارة الانتظار بنجاح على GitHub Pages.
الآن يمكنك مشاركة الرابط مع الآخرين واستخدام النظام فوراً!